/*const { assert } = require("chai");
const _deploy_contracts = require("../migrations/2_deploy_contracts");

const Auctuon = artifacts.require('Auction');

_deploy_contracts('Auction', () => {
    it('Should save bid', async () => {
        const auction = await Auction.deployed();
        const result = await auction.getHighestBid();
        assert(result === )
    })
})*/